apiVersion: v1
kind: Service
metadata:
  name: jenkins-svc-dev
  namespace: dev
  labels:
    app: jenkins
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
    - port: 50000
      targetPort: 50000
  selector:
    app: jenkins
  type: LoadBalancer
